CSRF Full Form:
👉 Cross-Site Request Forgery

What is it (short):
CSRF is a web attack where a logged-in user's browser is tricked into sending unwanted requests to a trusted site — like transferring money or changing a password — without the user's consent.

🛡️ Prevent using CSRF tokens, SameSite cookies, or custom headers.

